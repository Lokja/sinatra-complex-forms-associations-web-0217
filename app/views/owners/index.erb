<!DOCTYPE html>
<html>
  <head>
    <title>Owners</title>
  </head>

  <body>
    <% @owners.each do |owner| %>
      <h3><%= "#{owner.name} has the following pets:" %></h3>
      <% owner.pets.each do |pet| %>
        <li><%=pet.name%></li>
      <% end %>
    <% end %>
  </body>
</html>
